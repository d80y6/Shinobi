/**
 * Entry point for mediasoup-client browser bundle
 * Exports the Device class and other necessary components
 */
const mediasoupClient = require('mediasoup-client');

// Export everything to global scope
module.exports = mediasoupClient;
